{% extends 'base.html.twig' %}

{% block title %}Club{% endblock %}

{% block body %}
    <h1>Club</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ club.id }}</td>
            </tr>
            <tr>
                <th>NomClub</th>
                <td>{{ club.nomClub }}</td>
            </tr>
            <tr>
                <th>SportClub</th>
                <td>{{ club.sportClub }}</td>
            </tr>
            <tr>
                <th>AdresseClub</th>
                <td>{{ club.adresseClub }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_club_index') }}">Retour à la liste</a><br>

    <a href="{{ path('app_club_edit', {'id': club.id}) }}">Modifier</a>

    {{ include('club/_delete_form.html.twig') }}
{% endblock %}